# Simple Node.js App

A simple Node.js application created for Docker and DevOps practice.

## Features

- Simple Express server
- `/` endpoint
- `/health` endpoint
- Runs on port 3000

## Run locally

```bash
npm install
npm start